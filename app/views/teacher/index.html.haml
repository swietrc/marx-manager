%h1 Bonjour M. #{current_user.first_name} #{current_user.last_name}

%table{ :class => "table table-striped table-hover" }
  %thead
    %tr
      %th Titre
      %th Date de début
      %th Date de fin
  %tbody
  - if@subjects
    - @subjects.each do |s|
      %tr
        %td
          = link_to s.name, s
        %td
          = s.start_date
        %td
          = s.finish_date

= link_to "Ajouter un cours", new_subject_path, class: "btn btn-default"
